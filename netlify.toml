[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  timeout = 26

[functions."*"]
  timeout = 26

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
